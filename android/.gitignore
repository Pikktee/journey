# Gradle/Android-Build-Artefakte
.gradle/
build/
local.properties
# IDE
.idea/
*.iml
.kotlin/
# Firebase-Konfiguration (Push): ein Schlüssel, kein Quelltext. Lokal von Hand
# hinterlegt, in der CI aus einem Secret — s. docs/ops/push-einrichten.md.
app/google-services.json

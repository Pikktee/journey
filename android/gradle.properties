# JVM-Speicher für den Gradle-Daemon (Compose/KSP brauchen Luft)
org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
# AndroidX ist Pflicht, Jetifier braucht niemand mehr
android.useAndroidX=true
# Nur benötigte Module konfigurieren
org.gradle.configureondemand=true
org.gradle.caching=true
